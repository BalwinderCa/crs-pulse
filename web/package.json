{
  "name": "crs-pulse-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static legal pages (privacy + terms) for CRS Pulse, rendered from docs/*.md.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "marked": "^12.0.2"
  }
}
