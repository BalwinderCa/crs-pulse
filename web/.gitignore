node_modules
public
.vercel
# The site no longer ships an Apple Product Bezels image — the phone frame in
# PHONE_MOCK is drawn in CSS. Kept ignored so a licensed bezel dropped here for
# local experiments can never reach this public repo's history.
assets/frame
