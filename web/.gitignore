node_modules
public
.vercel
# Apple's official Product Bezels asset — license restricts redistribution;
# used locally + deployed to Vercel (which uploads based on this file, not git),
# but must not enter this public repo's history. See web/build.mjs comments.
assets/frame
