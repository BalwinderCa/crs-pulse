steps:
  - echo: Open on the Home hub - score, tracker and latest draws.
  - launch: com.crspulse.app
  - await: { visible: { text: Recent Draws } }
  - wait: 1000
  - await: { idle: true }
  - wait: 1200
